interface:
  display_name: "Review Push"
  short_description: "按实际 diff 提交推送；简单资产走快车道，逻辑变更先审查"
  default_prompt: "请对当前分支执行 review-push。简单项目资产（图、字体、静态资源及接入映射）按实际 diff 写提交信息后直接提交并推送；其余变更先按 merge-review 审查。本轮若已要求推送，提交并 fetch 后立刻 push。"
