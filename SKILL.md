---
name: review-push
description: 用户主动触发的审查与提交推送流程。逻辑变更先按 merge-review 再提交推送；简单项目资产（图、字体、静态资源及仅用于接入这些资源的映射）按实际 diff 写提交信息后直接提交并推送。同一轮口头「推送 / 提交并推送」即视为 push 授权。
---

# Review Push

这是 `merge-review` 的写入型衍生流程。它只用于用户明确要求提交并推送的本地 Git 仓库。

先看工作区，再选车道。完成标准：已选定一条车道，且该车道的结束条件已满足。

## 车道

**资产快车道** — 当用户要提交推送，且拟提交文件全部是项目资产（图片、字体、音视频、静态示意图）以及只为接入这些资产而改的映射/展示接线（import、`src`、图例表、替换 canvas 示意等），没有新的业务规则、请求、鉴权或状态机时：跳过 merge-review。根据实际 diff 写 Conventional Commits，只暂存这些文件，提交，fetch 当前分支，再 push 到已有 upstream。

**全量审查车道** — 其余变更：先跑 merge-review，通过后再走 Git 写入。

**口头授权** — 用户在本轮明确说推送、提交并推送、直接推上去时，该话本身就是本次 push 确认。按选定车道做完本地提交和 fetch 后立刻 push。

## 安全门槛

- 只操作用户明确指定的本地仓库、当前分支和远程；不自行切换分支。
- 全量审查车道发现 `Critical`、`Warning` 或未解决的 `Question` 时，停止在报告阶段，不提交、不 fetch、不 push。
- 不执行项目代码、测试、构建、安装脚本或未知仓库脚本。可以运行 Git 只读命令和用户明确指定的 Git 检查命令。
- 提交前检查工作区和暂存区。与本次范围无关的用户修改（含 `.env`、凭据）保持未暂存，说明后继续提交范围内文件；不要替用户清理或覆盖无关改动。
- 不使用 `git reset --hard`、`git clean`、`git checkout --`、强制 push 或自动覆盖远端历史。
- 不把 `.env`、密钥、私钥打进提交。
- push 目标是当前分支已跟踪的 upstream。无 upstream 时停止并说明。口头授权覆盖「push 前再问一次」；没有口头授权且走全量审查车道时，push 前展示远程、分支、SHA 和摘要并等待确认。

## 资产快车道步骤

完成标准：范围内文件已提交；已 fetch 并处理领先/落后/分叉；已 push 到 upstream（或因冲突/无 upstream 停止并说明）。

1. 用下方「检查仓库状态」命令确认分支、upstream、无冲突。
2. 列出拟提交文件。范围外文件保持未暂存。
3. 根据实际 diff 写提交信息（见「生成提交信息」），随即 `git add` 范围内文件并 commit。全量审查车道才「先展示草案再等用户改字」；本车道写完就提交。
4. fetch 当前分支并按第 4 节处理领先/落后/分叉。
5. `git push <remote> HEAD:<branch>`。报告 SHA、远程与结果。

## 前置审查：复用 merge-review 方法

仅全量审查车道执行。读取同目录的 `../merge-review/SKILL.md`，执行其只读审查流程。审查必须完成以下事项：

1. 确认当前分支、工作区状态、变更范围和关联需求。
2. 枚举全部变更文件，不把某个 diff 锚点当成完整变更。
3. 将文件分类为：逻辑、数据结构、常量/枚举、展示/文案、测试/配置。
4. 深审逻辑主变更及其直接依赖，检查边界条件、异常、并发、兼容性和副作用。
5. 输出简短结论、重点问题、影响模块和人工复核清单。

审查结束条件：所有变更文件已经分类；高优先级风险有明确结论；没有未解决的阻塞问题；用户提供的需求或 spec 已完成对照。审查结论不满足这些条件时不得进入 Git 写入阶段。

## Git 写入流程

### 1. 检查仓库状态

依次读取：

```bash
git rev-parse --show-toplevel
git branch --show-current
git status --short
git diff --stat
git diff --cached --stat
git remote -v
git branch -vv
```

确认：

- 当前目录是目标仓库。
- 当前分支不是 detached HEAD。
- 拟暂存文件属于本次范围；范围外文件保持未暂存并在报告里点名。
- 当前分支存在明确的 upstream；没有 upstream 时先停止并说明目标不明确。
- 没有未解决的 merge/rebase 冲突。

### 2. 生成提交信息

根据实际改动选择 Conventional Commits 类型：

- `feat`：新增用户可见能力
- `fix`：修复错误或不正确行为
- `refactor`：不改变外部行为的结构调整
- `test`：只增加或调整测试
- `docs`：只修改文档
- `chore`：构建、配置、依赖或工具链

提交信息语言规则：

- 标题开头的 Conventional Commits 类型枚举必须使用英文：`feat`、`fix`、`refactor`、`test`、`docs`、`chore`。
- 类型后面的 scope、标题描述和正文一律使用简体中文；不要把变更说明写成英文。
- Markdown 无头列表中的每一条内容都使用中文，描述实际变更、行为影响或验证结果。

标题格式：

```text
<type>(<可选中文 scope>): <中文动词短语>
```

标题使用对应类型开头、中文动词开头，说明目的而不是罗列文件，尽量不超过 72 个字符。正文使用 Markdown 无头列表，一条描述一个实际变更，说明行为或影响：

```text
fix(示例范围): 这里仅展示格式，不代表实际提交内容

- 示例条目一：仅用于说明正文格式
- 示例条目二：实际提交时必须替换
- 示例条目三：不得照抄示例内容
```

上面的代码块是格式示例，不得作为默认提交内容，也不得复制其中的 scope、标题措辞或列表条目。每次都必须根据当前实际 diff、变更分类、审查结论和真实影响重新生成提交信息；如果当前变更与示例主题无关，提交信息中不得出现“三点照合”或示例中的错误分类。不要在 commit message 中写未经验证的结论，不要把审查意见、临时调试信息或完整 diff 粘进去。全量审查车道：生成后先展示完整 commit message，再创建提交。资产快车道或本轮已口头授权推送时：按实际 diff 写好即提交，不额外等用户改提交说明。

### 3. 创建本地提交

提交前再次读取 `git diff` 和 `git diff --cached`，确认没有敏感信息和无关文件。只暂存属于本次变更的文件；若用户已经明确暂存了本次变更，可以保留暂存状态。

使用非交互命令创建提交，例如：

```bash
git add -- <explicit-file-list>
git commit -m "<subject>" -m "- item one\n- item two"
```

提交后记录新 commit SHA，并读取：

```bash
git show --stat --oneline --summary HEAD
git status --short
```

如果提交钩子失败，停止并报告钩子输出；不要跳过钩子，除非用户明确要求。

### 4. fetch 当前分支并检查远端变化

先解析 upstream，得到远程名和分支名，然后只 fetch 当前分支：

```bash
git fetch <remote> <branch>
git rev-list --left-right --count HEAD...<remote>/<branch>
```

解释结果：

- `0 0`：本地和远端一致，说明刚才的 commit 可能已经被某种流程同步，先停止并检查。
- `1 0` 或本地领先：继续 push。全量审查且本轮没有口头授权时，push 前再确认一次；资产快车道或已口头授权时直接 push。
- `0 N`：远端有本地没有的提交。优先执行 `git rebase <remote>/<branch>` 将本地提交放到最新远端之上；如果出现冲突，停止并交给用户处理，不自动猜测解决方案。
- 两边都大于 0：发生分叉，停止并要求用户选择 rebase 或 merge；不要自动创建 merge commit。

fetch 只更新远程跟踪引用，不能单独防止冲突；必须比较提交图，并在远端前进时整合后再 push。

### 5. 推送

执行：

```bash
git push <remote> HEAD:<branch>
```

报告中写明 remote 名称和 URL（隐藏 token）、目标分支、commit SHA 与标题、`git log <remote>/<branch>..HEAD --oneline`（push 前列表）、工作区是否干净。

全量审查且本轮没有口头授权时：先展示上述信息，等用户确认后再 push。资产快车道或本轮已口头授权时：展示与 push 同一次完成，确认就是那句口头授权。

禁止 `--force`、`--force-with-lease` 和推送到未跟踪、未说明的分支。push 失败时保留本地提交，报告失败原因和下一步，不重复盲推。

## 输出格式

使用简体中文，保持主次清晰。资产快车道把「审查结论」写成 `资产快车道：已按实际 diff 提交并推送`（或因 Git 状态停止的原因），可省略变更分类里的空项。

```markdown
## 审查结论
[通过 / 阻塞 / 资产快车道；只列最高优先级原因]

## 变更分类
- 逻辑主变更：...
- 数据结构：...
- 常量/枚举：...
- 展示/文案：...
- 测试/配置：...

## Commit 草案
```text
<conventional subject>

- <change>
- <change>
```

## Git 状态
- 当前分支：...
- 工作区：...
- fetch 结果：...
- 是否需要整合远端：...

## 推送结果
- 提交 SHA：...
- remote/branch：...
- 结果：已推送 / 等待确认 / 因冲突停止 / 推送失败
```

全量审查未通过时，输出风险和人工复核清单；资产快车道未成功 push 时写明停止原因。
